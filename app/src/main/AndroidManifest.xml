<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.github.iplanetcn.app.fruits">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackComposeAppFruits"
        tools:ignore="AllowBackup">
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/title_activity_settings"
            android:theme="@style/Theme.JetpackComposeAppFruits.NoActionBar" />
        <activity
            android:name=".FruitDetailActivity"
            android:exported="true"
            android:label="@string/title_activity_fruit_detail"
            android:theme="@style/Theme.JetpackComposeAppFruits" />
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:label="@string/title_activity_onboarding"
            android:theme="@style/Theme.JetpackComposeAppFruits" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.JetpackComposeAppFruits" />
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.JetpackComposeAppFruits.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>